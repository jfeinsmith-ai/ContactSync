# ContactSync

ContactSync is a native Android app for an on-demand, one-way copy of contacts
between two Google accounts already present on the device. It uses Android's
Contacts Provider only: there is no backend, Google OAuth client, automatic
schedule, or Internet permission.

Development is specification-driven. See [ContactSync Spec.md](ContactSync%20Spec.md).

